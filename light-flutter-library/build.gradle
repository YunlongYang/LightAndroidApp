apply from:rootProject.file("gradle_script/common_library_build.gradle").absolutePath

if(project.hasProperty("flutter.sdk")){
    String path = project.properties.get("flutter.sdk")
    dependencies {
        implementation files(path+"/bin/cache/artifacts/engine/android-arm/flutter.jar")
    }
}
